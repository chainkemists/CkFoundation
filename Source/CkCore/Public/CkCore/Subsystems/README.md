# CkCore / Subsystems

`UCk_GameWorldSubsystem` base class — standardizes world-subsystem registration + on-world-ready hook. Derive from this instead of `UWorldSubsystem` directly.

**Key files:** `GameWorldSubsytem/` subfolder

For the full use-case table and how this folder fits with the rest of CkCore, see [`../../Claude.md`](../../Claude.md).
