# CkCore / Game

Generic game-state and game-mode helpers that don't belong in the `Engine/` bases — `UCk_Utils_Game_UE` exposes shortcuts for "is server / is listen host / is standalone" style queries.

**Key files:** `CkGame_Utils.h`

For the full use-case table and how this folder fits with the rest of CkCore, see [`../../Claude.md`](../../Claude.md).
