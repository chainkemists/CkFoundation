# CkCore / Entt

Include-wrapper for the EnTT library (`Entt.h`). Use only from CkEcs internals; game code should go through `FCk_Handle_*` and CkEcs utilities.

**Key files:** `Entt.h`

For the full use-case table and how this folder fits with the rest of CkCore, see [`../../Claude.md`](../../Claude.md).

## See also
- `CkEcs/` module — the public ECS API that wraps EnTT.
