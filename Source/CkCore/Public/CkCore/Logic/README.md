# CkCore / Logic

`FCk_Condition` — data-driven condition evaluation returning `ECk_ConditionResult::{Pass,Fail}`. Use for configurable rule gates (interaction gates, quest pre-reqs) instead of hardcoded bool checks.

**Key files:** `CkCondition.h`

For the full use-case table and how this folder fits with the rest of CkCore, see [`../../Claude.md`](../../Claude.md).
