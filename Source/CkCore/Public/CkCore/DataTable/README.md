# CkCore / DataTable

`UDataTable` row-lookup helpers — typed row fetch, row-by-tag, safe column reads. Use these over `FindRow<T>` + null-check patterns.

**Key files:** `CkDataTable_Utils.h`

For the full use-case table and how this folder fits with the rest of CkCore, see [`../../Claude.md`](../../Claude.md).
