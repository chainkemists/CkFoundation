# CkCore / Functional

Small `ck::comparators` functors for use with `std::*` / `Algo::` / ranges APIs. Canonical example: `GameplayTag_MatchesTagExact` for tag-set dedup/sort.

**Key files:** `CkFunctional.h`

For the full use-case table and how this folder fits with the rest of CkCore, see [`../../Claude.md`](../../Claude.md).
