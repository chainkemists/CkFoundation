# CkCore / Concepts

C++20 concepts reused across CkFoundation templates. Pulled into `template <Concept T>` constraints instead of writing repeated `std::enable_if_t` / SFINAE.

**Key files:** `CkConcepts.h`

For the full use-case table and how this folder fits with the rest of CkCore, see [`../../Claude.md`](../../Claude.md).
