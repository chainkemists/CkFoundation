# CkCore / Curve

`FRuntimeFloatCurve` sampling helpers — safe evaluate (returns `TOptional`/default on invalid), normalization, inverse lookup.

**Key files:** `CkCurve_Utils.h`

For the full use-case table and how this folder fits with the rest of CkCore, see [`../../Claude.md`](../../Claude.md).
