# CkCore / TypeTraits

`ck::type_traits` namespace — `AsArray`, `AsString`, `Const` / `NonConst` tag types, `ExtractValueType<T>` (for `T*`, `TSharedPtr<T>`, container types), and other template-dispatch primitives.

**Key files:** `CkTypeTraits.h`

For the full use-case table and how this folder fits with the rest of CkCore, see [`../../Claude.md`](../../Claude.md).
