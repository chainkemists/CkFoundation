# CkCore / LevelStreaming

`ULevelStreaming`-based load/unload helpers with async completion callbacks. Use these instead of raw `UGameplayStatics::LoadStreamLevel`.

**Key files:** `CkLevelStreaming_Utils.h`

For the full use-case table and how this folder fits with the rest of CkCore, see [`../../Claude.md`](../../Claude.md).
