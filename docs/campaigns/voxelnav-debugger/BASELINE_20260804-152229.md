# Baseline - 2026-08-04 15:22:29 PDT

This is the authoritative pre-change regression snapshot for the VoxelNav debugger campaign.

## Invocation

Launched once through the detached UnrealToolbox workflow with its visible progress window:

```powershell
CkAuto/UnrealToolbox.exe --build --target=Editor --test --output=Saved/Logs/Baseline-VoxelNav3DDebugger.log --project=E:\Repos\CkPlugins_Other
```

## Result

- Editor build: succeeded.
- Tests discovered and run: 982.
- Passed: 974.
- Failed: 8.
- Skipped: 0.
- Contaminated: 0.
- Test duration: 3 minutes 28 seconds.
- Full log: `E:\Repos\CkPlugins_Other\Saved\Logs\Baseline-VoxelNav3DDebugger.log`.

## Named failing set

1. `Ck_AutoTest_PathNetworkFollower_ComponentTransferUsesDisconnectedIslands`
2. `Ck_AutoTest_PathNetworkFollower_DesiredNavmeshClearanceMovesInward`
3. `Ck_AutoTest_PathNetworkFollower_LocalShortcutUsesSameComponentGap`
4. `Ck_AutoTest_PathNetworkFollower_ProjectsRibbonWaypointWithinNavQueryExtent`
5. `Ck_AutoTest_PathNetworkFollower_RoutePrefersNetwork`
6. `Ck_AutoTest_PathNetworkFollower_TuningReplansSameGoal`
7. `Ck_AutoTest_StateMachine_DivergenceFirstBranch`
8. `SurfaceOffset`

The first six are functional-test assertions reported by `FinishTest`. The final two are also
reported as `Result={Fail}` by the automation controller. There were no contaminated tests.

## Final comparison rule

The campaign's final full build/all-tests gate must have:

- a successful editor build;
- zero contaminated tests;
- no failure outside the named set above;
- all newly added focused tests passing;
- no new ensure, AngelScript, or debugger-lifetime failure attributable to this campaign.

If one of the named failures becomes green, preserve that improvement. Do not use the baseline to
permit its reintroduction later in the campaign.
